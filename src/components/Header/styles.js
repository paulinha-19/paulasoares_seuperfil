import { theme } from "../../theme";

export const styles = {
    container: {
        backgroundColor: theme.palette.primary.dark,
        paddingTop: "40%",
        justifyContent: 'center',
        position: 'relative'
    },
};
